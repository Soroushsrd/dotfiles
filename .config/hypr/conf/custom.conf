# Add your additional Hyprland configurations here
#
# This is an additional key binding
# bind = $mainMod CTRL, up, workspace, empty
#
# Example for xwayland
# xwayland {
#   force_zero_scaling = true
# }

# qt5ct environment variable
# env = QT_QPA_PLATFORMTHEME,qt5ct

# SDL version
env = SDL_VIDEODRIVER,wayland
# env = SDL_VIDEODRIVER,x11

# No Hardware Cursor
# cursor {
#     no_hardware_cursors = false
# }

# Blur for waybar
#layerrule = blur, waybar
input {
    kb_layout = us,ir
    kb_variant =
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =
}
# HyprScroller Configuration

# Set the layout to use HyprScroller
# general {
#     layout = scroller
# }
#
# # Plugin configuration
# plugin {
#     hyprscroller {
#         # Column and window defaults
#         column_default_width = onehalf
#         window_default_height = one
#
#         # Cycle through these sizes when resizing
#         column_widths = onethird onehalf twothirds one
#         window_heights = onethird onehalf twothirds one
#
#         # Scrolling settings
#         speed = 6.0
#         smooth_scrolling = 1
#         invert = 0
#         acceleration = 1
#
#         # Focus behavior
#         focus_wrap = true
#
#         # Overview settings
#         overview_scale_content = true
#
#         # Gesture support
#         gesture_sensitivity = 1.0
#         gesture_scroll_enable = true
#         gesture_scroll_fingers = 3
#         gesture_overview_enable = true
#         gesture_overview_fingers = 4
#         gesture_workspace_switch_enable = true
#         gesture_workspace_switch_fingers = 4
#     }
# }
#
# # Key bindings for HyprScroller
# # Basic mode switching
# bind = $mainMod, bracketleft, scroller:setmode, row
# bind = $mainMod, bracketright, scroller:setmode, col
#
# # Navigation to beginning/end
# bind = $mainMod, Home, scroller:movefocus, begin
# bind = $mainMod, End, scroller:movefocus, end
#
# # Resizing columns/windows
# bind = $mainMod CTRL, equal, scroller:cyclewidth, next
# bind = $mainMod CTRL, minus, scroller:cyclewidth, prev
# bind = $mainMod CTRL SHIFT, equal, scroller:cycleheight, next
# bind = $mainMod CTRL SHIFT, minus, scroller:cycleheight, prev
#
# # Window organization
# bind = $mainMod, I, scroller:admitwindow,
# bind = $mainMod, O, scroller:expelwindow,
#
# # Overview mode
# bind = $mainMod, F12, scroller:toggleoverview
#
# # Window alignment
# bind = $mainMod ALT, C, submap, center
# submap = center
# bind = , C, scroller:alignwindow, c
# bind = , C, submap, reset
# bind = , M, scroller:alignwindow, m
# bind = , M, submap, reset
# bind = , right, scroller:alignwindow, r
# bind = , right, submap, reset
# bind = , left, scroller:alignwindow, l
# bind = , left, submap, reset
# bind = , escape, submap, reset
# submap = reset
#
# # Fit windows to screen
# bind = $mainMod, Z, submap, fitsize
# submap = fitsize
# bind = , W, scroller:fitsize, visible
# bind = , W, submap, reset
# bind = , right, scroller:fitsize, toend
# bind = , right, submap, reset
# bind = , left, scroller:fitsize, tobeg
# bind = , left, submap, reset
# bind = , up, scroller:fitsize, active
# bind = , up, submap, reset
# bind = , down, scroller:fitsize, all
# bind = , down, submap, reset
# bind = , escape, submap, reset
# submap = reset
#
# # Pin important columns
# bind = $mainMod ALT, P, scroller:pin,
#
# # Quick navigation with marks
# bind = $mainMod, apostrophe, submap, marksvisit
# submap = marksvisit
# bind = , A, scroller:marksvisit, a
# bind = , A, submap, reset
# bind = , B, scroller:marksvisit, b
# bind = , B, submap, reset
# bind = , C, scroller:marksvisit, c
# bind = , C, submap, reset
# bind = , escape, submap, reset
# submap = reset
#
# bind = $mainMod SHIFT, apostrophe, submap, marksadd
# submap = marksadd
# bind = , A, scroller:marksadd, a
# bind = , A, submap, reset
# bind = , B, scroller:marksadd, b
# bind = , B, submap, reset
# bind = , C, scroller:marksadd, c
# bind = , C, submap, reset
# bind = , escape, submap, reset
# submap = reset
#
# # Quick jump between windows
# bind = $mainMod, slash, scroller:jump,
#
# # Ensure HyprScroller is loaded at startup
# exec-once = hyprpm reload -n
# exec-once = hyprctl plugin load scroller
